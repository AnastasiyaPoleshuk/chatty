export interface IPriceCardInfo {
  id: string;
  title: string;
  description: string;
  duration: number;
  cost: number;
}

export interface ICalendarInfo {
  id: string;
  name: string;
  date: string;
  color: string;
}
